import 'package:flutter/material.dart';

class AppColors {
  // Main theme colors
  static const backgroundColor = Color(0xFF1A202C);
  static const accentColor = Color(0xFF4A90E2);
  static const cardColor = Color(0xFF2D3748);

  // Text colors
  static const textColor = Color(0xFFE2E8F0);
  static const secondaryTextColor = Color(0xFF718096);

  // Border colors
  static const borderColor = Color(0xFF2D3748);
  static const borderActiveColor = Color(0xFF3F5E7B);

  // State colors
  static const successColor = Color(0xFF48BB78); // Green for income
  static const warningColor = Color(0xFFED8936); // Orange for warnings
  static const dangerColor = Color(0xFFF56565); // Red for expenses/errors

  // Button hover/active state
  static const accentHoverColor = Color(0xFF6CB2EB);
}
